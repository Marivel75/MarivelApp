<p id="notice"><%= notice %></p>

<%= render 'POIdetails' %>

<div class="container">
  <div class="flex-container">
    <div class="navbar-item">
      <%= link_to "Éditez le lieu", edit_trip_place_path(@trip.id, @place[:id]), class:"button button-primary" %>
    </div>
    <div class="navbar-item">
      <%= link_to "Revenir au trip", trip_path(@trip), class:"button" %>
    </div>
  </div>
</div>
<br>
<br>
