<%= form_with(model: join_tag_to_place, local: true) do |form| %>
  <% if join_tag_to_place.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(join_tag_to_place.errors.count, "error") %> prohibited this join_tag_to_place from being saved:</h2>

      <ul>
      <% join_tag_to_place.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
