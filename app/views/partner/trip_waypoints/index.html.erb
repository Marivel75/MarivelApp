<br>
<br>

<div class="container">
  <h1>Liste des lieux</h1>
  <p>Parcourez la liste ci-dessous et ajouter des lieux à votre itinéraire</p>
  <p>Si vous ne trouvez pas ce que vous cherchez, vous avez la possibilité de créer un lieu personnalisé :</p>
  <%= link_to 'Créer un lieu', new_partner_place_path, class:"button button-primary" %>
  <br>
  <br>

  <table class="u-full-width">
    <thead>
      <tr>
        <th>ID</th>
        <th>Name</th>
        <th>Date de création</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @places.each do |place| %>
        <tr>
          <td><%= place.id%></td>
          <td><%= place.name %></td>
          <td><%= place.created_at%></td>
          <td><%= link_to 'Détails du lieu', partner_place_url(place[:id]) %></td>
          <td><%= button_to 'Ajouter', partner_trip_waypoint_index_path(place_id: place.id) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'Créer un lieu', new_partner_place_path, class:"button button-primary" %>
</div>
