<%= form_with(model: trip_waypoint, local: true) do |form| %>
  <% if trip_waypoint.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trip_waypoint.errors.count, "error") %> prohibited this trip_waypoint from being saved:</h2>

      <ul>
      <% trip_waypoint.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :place_id %>
    <%= form.text_field :place_id %>
  </div>

  <div class="field">
    <%= form.label :trip_id %>
    <%= form.text_field :trip_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
